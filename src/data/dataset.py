import tarfile
import requests
import gzip
import shutil
from tqdm import tqdm
import pandas as pd
import logging
import os


LOGGER = logging.getLogger('cli')


def download_dataset(datasets: list = None, path: str = "data/tweets"):
	# ...
	pass



def import_data(path: str = "data/tweets"):
	# ...
	pass